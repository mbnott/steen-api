DROP DATABASE IF EXISTS Steen;
CREATE DATABASE Steen;
USE Steen;

-- INSERT DEDICATED DB USER
DROP USER IF EXISTS SteenUser@localhost;
FLUSH PRIVILEGES;
CREATE USER SteenUser@localhost IDENTIFIED BY 'admin';
GRANT ALL PRIVILEGES ON Steen.* TO SteenUser@localhost;
FLUSH PRIVILEGES;
